/**
 * Classed used to represent the scope table.
 */

(function (Backbone, Compiler) {

	var ScopeTable = Backbone.Model.extend({

	});

	Compiler.ScopeTable = ScopeTable;

})(Backbone, Compiler);

// Set up class structure for the scope table